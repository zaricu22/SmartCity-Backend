# Test profile — datasource is overridden at runtime via @DynamicPropertySource (Testcontainers)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
server.servlet.context-path=/SmartCityREST
management.endpoints.enabled-by-default=false
