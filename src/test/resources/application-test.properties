spring.config.activate.on-profile=test
# Datasource is overridden at runtime via @DynamicPropertySource (Testcontainers)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
server.servlet.context-path=/SmartCityREST
management.endpoints.enabled-by-default=false
# Placeholder — overridden by @DynamicPropertySource in WireMock tests
energy-price.base-url=http://localhost
app.jwt.secret=${TEST_JWT_SECRET}
app.security.admin.password=${TEST_ADMIN_PASSWORD}
app.security.viewer.password=${TEST_VIEWER_PASSWORD}
