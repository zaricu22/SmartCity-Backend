<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.10</version>
        <relativePath/>
    </parent>
    <groupId>com.example</groupId>
    <artifactId>smartcityback</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>smartcityback</name>
    <description>smartcityback</description>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2024.0.1</spring-cloud.version>
        <spring-cloud-contract.version>4.2.0</spring-cloud-contract.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Required explicitly: Spring Boot 3.x no longer pulls this in transitively.
             PIT's JUnit 5 plugin needs LauncherFactory from this jar to discover tests. -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-contract-verifier</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.3.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.12</version>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>8.3.0</version>
        </dependency>
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>7.4</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.30</version>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.9</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.13.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-core</artifactId>
            <version>8.10.1</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <!-- BUNDLE = whole project as one unit, not per-class or per-package.
                                         Per-class rules cause false failures on small utility classes;
                                         bundle-level reflects overall test suite health. -->
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <!-- 70%: push-gate floor for the main pipeline. Intentionally below
                                                 PIT's 75% coverageThreshold — PIT and JaCoCo measure differently
                                                 (PIT counts only lines reachable by the test-compile scope).
                                                 Modest floor; not a ceiling. Raise when test suite matures. -->
                                            <minimum>0.70</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <!-- 60%: branch coverage is structurally harder to reach than line
                                                 coverage — error paths, guard clauses, and exception branches
                                                 often require specific integration or Testcontainers setup.
                                                 Set lower than line to avoid forcing trivial tests. -->
                                            <minimum>0.60</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                            <!-- Pure data carriers and framework wiring have no logic to test.
                                 Mutating a getter or a field assignment produces surviving mutants
                                 that indicate nothing about correctness — same exclusion list as PIT. -->
                            <excludes>
                                <exclude>**/*Dto.*</exclude>
                                <exclude>**/*Request.*</exclude>
                                <exclude>**/*Response.*</exclude>
                                <exclude>**/*Mapper.*</exclude>
                                <exclude>**/*Exception.*</exclude>
                                <exclude>**/config/**</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Activate with: mvn verify -Pgenerate-openapi -DskipTests
             Starts the app with the 'ci' profile (H2), hits /v3/api-docs, writes openapi.json to project root. -->
        <profile>
            <id>generate-openapi</id>
            <dependencies>
                <!-- H2 only needed when generating schema locally; not included in the production JAR -->
                <dependency>
                    <groupId>com.h2database</groupId>
                    <artifactId>h2</artifactId>
                    <scope>runtime</scope>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>pre-integration-test</id>
                                <goals>
                                    <goal>start</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>post-integration-test</id>
                                <goals>
                                    <goal>stop</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <profiles>
                                <profile>ci</profile>
                            </profiles>
                            <wait>1000</wait>
                            <maxAttempts>60</maxAttempts>
                        </configuration>
                    </plugin>

                    <plugin>
                        <groupId>org.springdoc</groupId>
                        <artifactId>springdoc-openapi-maven-plugin</artifactId>
                        <version>1.4</version>
                        <executions>
                            <execution>
                                <id>generate-openapi-docs</id>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <apiDocsUrl>http://localhost:8080/SmartCityREST/v3/api-docs</apiDocsUrl>
                            <outputFileName>openapi.json</outputFileName>
                            <outputDir>${project.basedir}</outputDir>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        
        <!-- Activate with: mvn test-compile org.pitest:pitest-maven:mutationCoverage -Pmutation-testing -->
        <profile>
            <id>mutation-testing</id>
            <build>
                <plugins>
                    <!-- Prevent Spring Boot from repackaging into a fat JAR so PIT can scan classes -->
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <configuration>
                            <classifier>exec</classifier>
                        </configuration>
                    </plugin>

                    <!-- Skip Surefire: PIT runs tests itself; running both wastes memory -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>

                    <!-- Skip JaCoCo: its Java agent conflicts with PIT's bytecode instrumentation -->
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>

                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>1.9.11</version>

                        <dependencies>
                            <!-- 1.2.x required: implements OutputDirectoryProvider for JUnit Platform 1.11+ -->
                            <dependency>
                                <groupId>org.pitest</groupId>
                                <artifactId>pitest-junit5-plugin</artifactId>
                                <version>1.2.2</version>
                            </dependency>
                        </dependencies>

                        <configuration>
                            <targetClasses>
                                <!-- Domain -->
                                <param>com.example.smartcityback.asset.domain.aggregate.*</param>
                                <param>com.example.smartcityback.asset.domain.entity.*</param>
                                <param>com.example.smartcityback.asset.domain.specification.*</param>
                                <param>com.example.smartcityback.asset.domain.valueobject.*</param>
                                <!-- Application -->
                                <param>com.example.smartcityback.asset.application.service.*</param>
                            </targetClasses>

                            <threads>1</threads>
                            <timeoutFactor>2</timeoutFactor>
                            <timeoutConstant>4000</timeoutConstant>

                            <outputFormats>
                                <param>HTML</param>
                                <param>XML</param>
                            </outputFormats>

                            <timestampedReports>false</timestampedReports>

                            <mutationThreshold>65</mutationThreshold>
                            <coverageThreshold>75</coverageThreshold>

                            <excludedClasses>
                                <param>*.dto.*</param>
                                <param>*.config.*</param>
                                <param>*.generated.*</param>
                                <param>*.mapper.*</param>
                                <param>*.request.*</param>
                                <param>*.response.*</param>
                                <param>*.exception.*</param>
                                <param>*.repository.*</param>
                            </excludedClasses>

                            <excludedTestClasses>
                                <param>*IntegrationTest</param>
                                <param>*TransactionalTest</param>
                                <param>*WireMockTest</param>
                                <param>*APITest</param>
                                <param>*WebSocket*</param>
                                <param>*FullIntegrationTest</param>
                                <param>*ApplicationTests</param>
                                <param>*RepositoryImplTest</param>
                            </excludedTestClasses>

                            <jvmArgs>
                                <jvmArg>-Xmx768m</jvmArg>
                                <jvmArg>--add-opens=java.base/java.lang=ALL-UNNAMED</jvmArg>
                                <jvmArg>--add-opens=java.base/java.util=ALL-UNNAMED</jvmArg>
                            </jvmArgs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
