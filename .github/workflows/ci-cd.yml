# 1. The name of your workflow
name: Backend CI/CD Pipeline

# 2. The TRIGGER: When should this run?
on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]

# 3. The JOBS: What should it do?
jobs:
  # -------------------------------------------------------------------------
  # JOB 1: SECURITY - The "Shield"
  # -------------------------------------------------------------------------
  security-static:
    name: Security Scan - Static
    # [OFFICIAL] GitHub-hosted runner, maintained by GitHub
    runs-on: ubuntu-latest 

    # 4. The STEPS: The individual commands
    steps:
      # [OFFICIAL] actions/checkout — fetch-depth: 0 required for Gitleaks to scan full history
      - uses: actions/checkout@v4
        with:
          # Required for Gitleaks to scan history
          fetch-depth: 0

      # Gitleaks: Scans commits for secrets (API keys, passwords)
      - name: Gitleaks secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # [OFFICIAL] github/codeql-action — SAST tool maintained by GitHub, looks for logic vulnerabilities
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java

      - name: Autobuild for CodeQL
        uses: github/codeql-action/autobuild@v3

      - name: CodeQL analysis
        uses: github/codeql-action/analyze@v3

  security-snyk-dependencies:
    name: SNYK Dependency Check
    runs-on: ubuntu-latest 
    steps:
      # [OFFICIAL] actions/checkout — clones the repo into the runner
      - uses: actions/checkout@v4

      # [OFFICIAL] actions/setup-java — installs JDK 17; 'cache: maven' caches ~/.m2 between runs
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Build app
        run: mvn -B clean package -DskipTests

      - name: Run Snyk Maven scan
        uses: snyk/actions/maven-3-jdk-17@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: >
            --severity-threshold=high
            --skip-unresolved

  security-owasp-dependencies:
    name: OWASP Dependency Check
    runs-on: ubuntu-latest 
    steps:
      # [OFFICIAL] actions/checkout — clones the repo into the runner
      - uses: actions/checkout@v4

      # [OFFICIAL] actions/cache — persists the NVD database between runs to avoid re-downloading it
      - name: Cache OWASP dependency-check data
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository/org/owasp
          key: owasp-java-${{ runner.os }}

      - name: Run OWASP Dependency Check
        uses: dependency-check/Dependency-Check_Action@main
        with:
          project: "smartcityback"
          path: "."
          format: "HTML"
          out: "reports"

      # [OFFICIAL] actions/upload-artifact — saves the HTML report as a downloadable artifact
      - name: Upload OWASP report
        uses: actions/upload-artifact@v4
        with:
          name: owasp-backend-report
          path: reports

  # -------------------------------------------------------------------------
  # JOB 2: TESTING - The "Validator"
  # -------------------------------------------------------------------------
  test:
    name: Build and Test
    runs-on: ubuntu-latest 
    needs: [ security-static, security-snyk-dependencies, security-owasp-dependencies ]
    steps:
      # [OFFICIAL] actions/checkout — clones the repo into the runner
      - uses: actions/checkout@v4

      # [OFFICIAL] actions/setup-java — installs JDK 17; 'cache: maven' caches ~/.m2 between runs
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Run tests with coverage
        run: mvn verify -e
        env:
          DB_URL: ${{ secrets.DB_URL }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

      # Codecov: Uploads test coverage reports to a visual dashboard
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: target/site/jacoco/jacoco.xml
          fail_ci_if_error: true
          verbose: true

      # [OFFICIAL] actions/upload-artifact — saves raw Surefire reports for debugging failures
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: target/surefire-reports/

  # -------------------------------------------------------------------------
  # JOB 3: CODE QUALITY - The "Linter"
  # -------------------------------------------------------------------------
  code-quality:
    name: Code Quality & Linting
    runs-on: ubuntu-latest 
    needs: [ security-static, security-snyk-dependencies, security-owasp-dependencies ]
    env:
      # Efficient GC, gives SonarCloud clean heap space
      MAVEN_OPTS: "-Xmx4g -XX:+UseG1GC"
    steps:
      # [OFFICIAL] actions/checkout — fetch-depth: 0 required for SonarCloud blame data
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # [OFFICIAL] actions/setup-java — installs JDK 17; 'cache: maven' caches ~/.m2 between runs
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      # [OFFICIAL] actions/cache — persists the SonarCloud scanner cache between runs
      - name: Cache SonarCloud packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: sonar-${{ runner.os }}

      # Checkstyle: Annotates PRs with style violations (spacing, naming, etc.)
      - name: Run Checkstyle
        uses: dbelyaev/action-checkstyle@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          checkstyle_config: checkstyle.xml
          level: error
          fail_level: error

      # PMD: Detects common programming flaws (empty catch blocks, unused code)
      - name: Run PMD
        uses: pmd/pmd-github-action@v2
        with:
          rulesets: 'rulesets/java/quickstart.xml'
          analyzeModifiedFilesOnly: false

      # SonarCloud: In-depth analysis of technical debt and code smells
      - name: SonarCloud Analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn verify sonar:sonar
          -Dsonar.projectKey=zaricu22_SmartCity-Backend
          -Dsonar.organization=zaricu22
          -Dsonar.host.url=https://sonarcloud.io
          -Dsonar.token=${{ secrets.SONAR_TOKEN }}

  # -------------------------------------------------------------------------
  # JOB 4: RELEASE & DEPLOY - The "Shipper"
  # Conditional: This job ONLY runs when code hits 'main'
  # -------------------------------------------------------------------------
  release-and-deploy:
    name: Semantic Release & Deploy
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest # [OFFICIAL] GitHub-hosted runner, maintained by GitHub
    environment: Render - production
    needs: [test, code-quality]
    steps:
      # [OFFICIAL] actions/checkout — GH_TOKEN used instead of GITHUB_TOKEN to allow tag pushing
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Semantic Release: Automates SemVer versioning and creates GitHub Releases
      - name: Run Semantic Release
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          npx \
            -p semantic-release \
            -p @semantic-release/commit-analyzer \
            -p @semantic-release/release-notes-generator \
            -p @semantic-release/github \
            semantic-release \
            --plugins @semantic-release/commit-analyzer \
            --plugins @semantic-release/release-notes-generator \
            --plugins @semantic-release/github
            
      # Deployment: Triggers the Render hosting service to pull new code
      - name: Trigger Render deployment
        run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}

      # Smoke Test: Pings the live URL every 20s to confirm successful deploy
      - name: Smoke Test - Health Check
        run: |
          echo "Waiting for Render to deploy (checking every 20s)..."
          for i in {1..20}; do
            if curl --fail --silent https://your-app-name.onrender.com/health; then
              echo "App is LIVE and healthy!"
              exit 0
            fi
            echo "Not ready yet, retrying in 20 seconds..."
            sleep 20
          done
          echo "Timeout: App failed to start in 400 seconds."
          exit 1
